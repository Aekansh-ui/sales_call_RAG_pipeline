# =============================================================================
# Sales Call RAG Pipeline — Python dependencies
# =============================================================================
# Pin all versions for reproducibility. Update deliberately, not accidentally.
# Heavy transcription packages (faster-whisper, pyannote.audio) are commented
# out here and will be uncommented in T-03 when we build the ingestion pipeline.
# They are excluded now so the initial `docker build` completes in < 2 minutes
# instead of downloading ~1.5 GB of model weights and C extensions.
# =============================================================================

# --- API server ---
fastapi==0.115.0        # async REST framework; generates OpenAPI docs automatically
uvicorn[standard]==0.30.6  # ASGI server that runs FastAPI; [standard] adds watchfiles + websockets

# --- UI ---
streamlit==1.38.0       # reactive data app framework; each rerun rerenders from top

# --- Database ---
psycopg2-binary==2.9.9  # PostgreSQL driver (binary = no libpq-dev needed in image)
pgvector==0.3.2         # Python adapter for the pgvector extension; registers vector type

# --- RAG / LLM framework ---
langchain==0.3.0
langchain-community==0.3.0  # integrations: Ollama, pgvector, loaders
langchain-ollama==0.2.0     # first-class Ollama chat + embeddings support

# --- Sentiment analysis ---
vaderSentiment==3.3.2   # lexicon-based; no model download, works fully offline

# --- Utilities ---
python-dotenv==1.0.1    # loads .env into os.environ at startup
rich==13.8.0            # beautiful progress bars and console output for ingest.py
numpy==1.26.4           # vector maths; pgvector and embeddings both return numpy arrays
httpx==0.27.2           # async HTTP client; used to call Ollama REST API directly

# --- Testing (T-07) ---
pytest==8.3.3
pytest-asyncio==0.24.0  # async test support for FastAPI route tests

# --- Transcription & diarization (T-03 — uncomment before T-03 build) ---
# faster-whisper==1.0.3       # CTranslate2-based Whisper; 4× faster than openai-whisper on CPU
# pyannote.audio==3.3.2       # speaker diarization; requires HuggingFace token (T-00 step 4)
